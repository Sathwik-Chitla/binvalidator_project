# --- CRITICAL TOP-LEVEL PACKAGES ---

# Deep Learning (NOTE: These versions imply a CUDA/GPU environment: cu121)
# You MUST ensure your Render service has a GPU plan, OR change these to CPU versions.
python==3.11.14
# --- CRITICAL TOP-LEVEL PACKAGES ---

# Deep Learning (CPU versions - Tightly coupled)
# Using a slightly older, highly stable PyTorch version (2.2.2) and its compatible TorchVision.
# If these versions are too old for your code, you can try 2.5.1 and 0.21.0, but 2.2.2 is safer.
torch==2.2.2
torchvision==0.17.2

# Computer Vision / YOLO
ultralytics==8.3.231
ultralytics-thop==2.0.18

# Streamlit App
streamlit==1.31.1
pydeck
altair

# OpenCV (Use HEADLESS for server/non-GUI deployment)
opencv-python-headless==4.9.0.80

# --- DATA SCIENCE / UTILITIES ---

# Core Data/ML
numpy>=1.26.4
pandas>=2.2.1
scikit-learn>=1.3.2
scipy
polars
pyarrow

# Visualization / Image
matplotlib>=3.8.3
Pillow>=10.2.0

# Other Utilities
PyYAML>=6.0.1
requests>=2.31.0
tqdm>=4.66.2
clip-anytorch==2.6.0
GitPython
rich
